# Wave 3, Ticket 3.2 — Add Structured LLM Output

Wave: LLM Syllabus Extraction

Status: Complete

## Goal

Constrain model output to the canonical course extraction schema.

## Implemented

- Added the official OpenAI JavaScript SDK and Responses API integration.
- Used responses.parse with zodTextFormat and the canonical Zod schema.
- Selected gpt-5.4-mini by default with an environment model override.
- Disabled response storage and separated system instructions from untrusted user content.
- Added configurable API key, client, model, and timeout inputs.
- Added stable missing-key, empty-output, and provider-failure errors.
- Kept provider errors out of user-facing messages.

## Tests

- Strict JSON-schema request construction and message roles.
- Default and explicit model selection.
- Missing API configuration.
- Missing parsed output.
- Safe provider-error mapping.

## Result

Complete. Five focused structured-output tests pass with typecheck and lint. No external API call is required by tests.

